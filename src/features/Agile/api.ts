// /projects/1/epics. epics
// /projects/1/issues?sprint=1
// /projects/1/issues

// Agile
// gantt(roadmap) => epic[] => (issue|milestone)[]
// board => sprint => issues[]
// Backlog

export const useProject = (id: string) => {};
// useQuery<IComment[]>(HOST, {
//   onError: console.log,
//   interceptor: undefined,
// });

export const useEpics = (id: string) => {};

export const useIssues = (id: string) => {};
